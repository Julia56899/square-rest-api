# скомпилированные классы и jar — генерируются при каждой сборке
target/

# личные настройки IDE — у каждого свои, в репо не нужны
.idea/workspace.xml

# системный файл macOS — создаётся сам
.DS_Store
